services:
  - type: web
    name: public-database-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    plan: free
    healthCheckPath: /health
    autoDeploy: true
